#!/bin/sh
#
#-----------------------------------------------------------------------

CUR_DIR=$PWD

NAME=geant4-v11.0.2

#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

cd EXT; # перейдем во внешнее хранилище 

#-----------------------------------------------------------------------
#

mkdir -p R/obj

cd R/obj


#cmake -DCMAKE_INSTALL_PREFIX=$CUR_DIR/R ../../$NAME  | tee  $CUR_DIR/cmake

#make -j2  2>&1 | tee   $CUR_DIR/make

make install 2>&1 | tee   $CUR_DIR/make_install

#-----------------------------------------------------------------------
#-----------------------------------------------------------------------
